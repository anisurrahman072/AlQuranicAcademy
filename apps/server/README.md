# Server (future)

Future REST API or backend service for Al-Qur'anic Academy.

**Not implemented yet.** The web app is static; no server code lives here in v1.

When you add a backend, place it in this app or a dedicated `packages/api` and wire it to `apps/web` or `apps/mobile` as needed.
